name: Damg
on:
  push:
    branches:
      - main
      - test
jobs: 
  build:
    name: Code-integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Create env
        run: |
          cd Application && \
          echo "${{ secrets.ENTIRE_ENV }}" > .env
      - name: Docker Intsall
        uses: docker-practice/actions-setup-docker@master
        timeout-minutes: 12
        run: |
          set -x

          docker version

          docker compose up --build


